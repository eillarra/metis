# Generated by Django 4.2.6 on 2023-10-26 06:33

import uuid

from django.db import migrations, models


def generate_uuids(apps, schema_editor):
    Evaluation = apps.get_model("metis", "Evaluation")

    # bulk save to skip auto_now_add
    updates = []

    for evaluation in Evaluation.objects.all():
        evaluation.uuid = uuid.uuid4()
        updates.append(evaluation)

    Evaluation.objects.bulk_update(updates, ["uuid"])


class Migration(migrations.Migration):
    dependencies = [
        ("metis", "0032_absence_boolnull_approve"),
    ]

    operations = [
        migrations.AddField(
            model_name="evaluation",
            name="uuid",
            field=models.UUIDField(default=uuid.uuid4, editable=False),
        ),
        # -----
        migrations.RunPython(generate_uuids),
        # -----
        migrations.AlterField(
            model_name="evaluation",
            name="uuid",
            field=models.UUIDField(default=uuid.uuid4, editable=False, unique=True),
        ),
    ]
